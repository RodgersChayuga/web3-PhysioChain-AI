{
    "builds": [
        {
            "src": "frontend/package.json",
            "use": "@vercel/next"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/frontend"
        }
    ],
    "installCommand": "pnpm install --prefix frontend",
    "buildCommand": "pnpm run build --prefix frontend",
    "outputDirectory": "frontend/.next"
}